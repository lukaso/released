{
  "name": "@released/core",
  "version": "0.0.0",
  "private": true,
  "description": "Find the first release containing a git commit — algorithm, GitHub + GitLab providers, parser, sanitizer. Pure TypeScript, internal workspace package: bundled into the `git-released` CLI tarball at publish time, and consumed by the Workers via the pnpm workspace symlink. Not published to npm.",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "import": "./dist/index.js"
    }
  },
  "files": ["dist", "src"],
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc -p tsconfig.json --noEmit"
  },
  "devDependencies": {
    "typescript": "^6.0.3",
    "vitest": "^4.1.7"
  },
  "dependencies": {
    "micromark": "^4.0.1"
  },
  "engines": { "node": ">=20" }
}
