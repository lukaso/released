---
"git-released": patch
---

Smoke-test release to validate the OIDC Trusted Publishing path end-to-end (CI publish via short-lived npm token + provenance attestation), now that npmjs.com → git-released has a Trusted Publisher rule for `lukaso/released` → `release.yml`.

CLI functionality identical to 0.1.1.
